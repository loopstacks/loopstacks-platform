export interface Agent {
  id: string;
  name: string;
  version: string;
  // TODO: Add full type definition
}

export interface LoopStack {
  id: string;
  name: string;
  version: string;
  // TODO: Add full type definition
}