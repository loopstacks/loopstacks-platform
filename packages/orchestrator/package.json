{
  "name": "@loopstacks/orchestrator",
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@loopstacks/registry-api": "workspace:*",
    "@loopstacks/schemas": "workspace:*",
    "@loopstacks/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}